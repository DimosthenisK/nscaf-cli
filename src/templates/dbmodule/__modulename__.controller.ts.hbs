import { Controller } from '@nestjs/common';
import { Crud } from '@nestjsx/crud';
import { {{moduleName.entityNameCapitalized}} } from './{{moduleName.entityName}}.dbentity';
import { {{moduleName.classified}}Service } from './{{moduleName.classified}}.service';

@Crud({
    model: {
        type: {{moduleName.entityNameCapitalized}},
    },
})
@Controller('{{moduleName.entityName}}')
export class {{moduleName.classified}}Controller {
    constructor(public service: {{moduleName.classified}}Service) {}
}
