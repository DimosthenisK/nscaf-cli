import { Test, TestingModule } from '@nestjs/testing';
import { {{moduleName.classified}}Service } from './{{moduleName.filenamed}}.service';

describe('{{moduleName.classified}}', () => {
  let service: {{moduleName.classified}};

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [{{moduleName.classified}}],
    }).compile();

    service = module.get<{{moduleName.classified}}>({{moduleName.classified}});
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
